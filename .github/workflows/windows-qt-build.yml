name: Build Windows bitcoin-qt.exe (MSYS2, Qt5 dynamic)

on:
  workflow_dispatch:

jobs:
  win-qt:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ------------------------------------------------------------
      # MSYS2 + MINGW64 + Qt5 (dynamic) + deps
      #  - miniupnpc는 설치하지 않음 (UPnP 빌드 제외)
      # ------------------------------------------------------------
      - name: Setup MSYS2 (MINGW64) + deps
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            git
            autoconf
            automake
            libtool
            pkgconf
            make
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-pkgconf
            mingw-w64-x86_64-qt5-base
            mingw-w64-x86_64-qt5-tools
            mingw-w64-x86_64-qt5-svg
            mingw-w64-x86_64-qt5-translations
            mingw-w64-x86_64-boost
            mingw-w64-x86_64-libevent
            mingw-w64-x86_64-qrencode
            mingw-w64-x86_64-zeromq
            mingw-w64-x86_64-libsodium
            zip
            unzip

      # ------------------------------------------------------------
      # 환경 검증 (팩트체크)
      # ------------------------------------------------------------
      - name: Verify Qt (import libs) & tools
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          pacman -Q | grep -E 'qt5-|qrencode|libevent|boost' || true

          # 동적 import libs는 반드시 존재해야 함
          ls -la /mingw64/lib/libQt5Core.dll.a
          ls -la /mingw64/lib/libQt5Gui.dll.a
          ls -la /mingw64/lib/libQt5Widgets.dll.a
          ls -la /mingw64/lib/libQt5Network.dll.a
          ls -la /mingw64/lib/libQt5DBus.dll.a
          ls -la /mingw64/lib/libqrencode.dll.a

          which g++
          g++ --version

          which moc
          which uic
          which rcc

          # windeployqt는 환경에 따라 없을 수 있음(없어도 빌드/패키징 가능: 아래에서 fallback)
          if command -v windeployqt >/dev/null 2>&1; then
            echo "windeployqt found: $(command -v windeployqt)"
          else
            echo "windeployqt NOT found (will use manual Qt DLL/plugin copy fallback)."
          fi

      # ------------------------------------------------------------
      # autogen
      # ------------------------------------------------------------
      - name: Autogen
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"
          ./autogen.sh

      # ------------------------------------------------------------
      # configure
      #  - 핵심: UPnP 빌드 제외 (--without-miniupnpc)
      #  - Qt5 GUI on
      #  - wallet/tests/bench/fuzz off
      # ------------------------------------------------------------
      - name: Configure (Qt5 GUI, NO UPNP)
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          export PKG_CONFIG="/mingw64/bin/pkg-config"
          export PKG_CONFIG_PATH="/mingw64/lib/pkgconfig:/mingw64/share/pkgconfig"

          MOC=/mingw64/bin/moc.exe
          UIC=/mingw64/bin/uic.exe
          RCC=/mingw64/bin/rcc.exe
          if [ -x /mingw64/bin/lrelease-qt5.exe ]; then
            LRELEASE=/mingw64/bin/lrelease-qt5.exe
          elif [ -x /mingw64/bin/lrelease.exe ]; then
            LRELEASE=/mingw64/bin/lrelease.exe
          else
            LRELEASE=
          fi

          MOC="$MOC" UIC="$UIC" RCC="$RCC" LRELEASE="$LRELEASE" \
            ./configure \
              --host=x86_64-w64-mingw32 \
              --with-gui=qt5 \
              --with-qt-incdir=/mingw64/include/qt5 \
              --with-qt-libdir=/mingw64/lib \
              --with-qt-bindir=/mingw64/bin \
              --disable-wallet \
              --disable-tests \
              --disable-bench \
              --disable-fuzz-binary \
              --without-miniupnpc \
              --without-natpmp

          # configure 결과 요약(로그)
          (grep -nE "with gui|with upnp|with natpmp|with qr" config.log || true)

      # ------------------------------------------------------------
      # !!! 핵심 패치 !!!
      # - Bitcoin Core autotools가 Windows에서 LIBTOOL_APP_LDFLAGS=-all-static을 넣는 케이스가 있음
      # - 이게 남아있으면 최종 링크가 -static 강제되어 동적 Qt(.dll.a)에서 무조건 터짐
      # - 해결: configure 이후에 src/Makefile.qt.include에서 -all-static 제거 (정확히 1곳)
      # ------------------------------------------------------------
      - name: Patch -all-static (the real source: src/Makefile.qt.include)
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          test -f src/Makefile.qt.include

          echo "---- BEFORE ----"
          grep -nE '^LIBTOOL_APP_LDFLAGS' src/Makefile.qt.include || true

          # 정확히 이 라인만 비움 (정적 강제 제거)
          sed -i 's/^LIBTOOL_APP_LDFLAGS[[:space:]]*=.*$/LIBTOOL_APP_LDFLAGS =/' src/Makefile.qt.include

          echo "---- AFTER ----"
          grep -nE '^LIBTOOL_APP_LDFLAGS' src/Makefile.qt.include || true

          # 검증: -all-static이 남아있으면 즉시 실패
          if grep -RIn -- "-all-static" src/Makefile.qt.include src/Makefile src/qt/Makefile 2>/dev/null; then
            echo "ERROR: -all-static still present."
            exit 1
          fi

      # ------------------------------------------------------------
      # 빌드 (링크 라인 출력)
      # ------------------------------------------------------------
      - name: Build bitcoin-qt.exe (V=1)
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          cd src

          # 진단용: 링크 라인에서 -static이 없어야 정상
          make -j1 V=1 qt/bitcoin-qt.exe

          ls -la qt/bitcoin-qt.exe

          echo "---- DLL deps (objdump) ----"
          x86_64-w64-mingw32-objdump -p qt/bitcoin-qt.exe | grep -i "DLL Name" || true

      # ------------------------------------------------------------
      # 패키징
      # 1) windeployqt 있으면 사용
      # 2) 없으면 수동 복사 fallback
      # ------------------------------------------------------------
      - name: Package (windeployqt if available, else manual Qt copy) + zip
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          rm -rf dist
          mkdir -p dist

          cp -a src/qt/bitcoin-qt.exe dist/bitcoin-qt.exe

          if command -v windeployqt >/dev/null 2>&1; then
            echo "Using windeployqt..."
            windeployqt --release --no-translations --compiler-runtime dist/bitcoin-qt.exe
          else
            echo "windeployqt not found -> manual Qt DLL + plugin copy fallback"

            # 최소 Qt 런타임 DLL 복사
            cp -a /mingw64/bin/Qt5Core.dll dist/ || true
            cp -a /mingw64/bin/Qt5Gui.dll dist/ || true
            cp -a /mingw64/bin/Qt5Widgets.dll dist/ || true
            cp -a /mingw64/bin/Qt5Network.dll dist/ || true
            cp -a /mingw64/bin/Qt5DBus.dll dist/ || true
            cp -a /mingw64/bin/libgcc_s_seh-1.dll dist/ || true
            cp -a /mingw64/bin/libstdc++-6.dll dist/ || true
            cp -a /mingw64/bin/libwinpthread-1.dll dist/ || true

            # 플랫폼 플러그인(qwindows) 없으면 GUI 실행 안 됨
            mkdir -p dist/platforms
            cp -a /mingw64/share/qt5/plugins/platforms/qwindows.dll dist/platforms/ || true

            # 스타일 플러그인(없어도 실행은 되지만, 일부 환경에서 경고 줄임)
            mkdir -p dist/styles
            if [ -f /mingw64/share/qt5/plugins/styles/qwindowsvistastyle.dll ]; then
              cp -a /mingw64/share/qt5/plugins/styles/qwindowsvistastyle.dll dist/styles/ || true
            fi
          fi

          # 결과 확인
          ls -la dist || true
          ls -la dist/platforms || true

          # zip
          powershell -NoProfile -Command "Compress-Archive -Path dist\* -DestinationPath bitcoinbt-qt-win64.zip"
          ls -la bitcoinbt-qt-win64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bitcoinbt-qt-win64
          path: bitcoinbt-qt-win64.zip
