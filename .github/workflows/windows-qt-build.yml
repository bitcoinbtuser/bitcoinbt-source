name: Build Windows bitcoin-qt.exe (MSYS2)

on:
  workflow_dispatch:

jobs:
  win-qt:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSYS2 (MINGW64) + deps
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            git
            autoconf
            automake
            libtool
            pkgconf
            make
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-pkgconf
            mingw-w64-x86_64-qt5-base
            mingw-w64-x86_64-qt5-tools
            mingw-w64-x86_64-qt5-declarative
            mingw-w64-x86_64-qt5-svg
            mingw-w64-x86_64-qt5-translations
            mingw-w64-x86_64-boost
            mingw-w64-x86_64-libevent
            mingw-w64-x86_64-zeromq
            mingw-w64-x86_64-libsodium
            mingw-w64-x86_64-qrencode
            mingw-w64-x86_64-miniupnpc

      - name: Verify Qt import libs exist
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          # must exist (dynamic import libs)
          ls -la /mingw64/lib/libQt5Core.dll.a
          ls -la /mingw64/lib/libQt5Gui.dll.a
          ls -la /mingw64/lib/libQt5Widgets.dll.a
          ls -la /mingw64/lib/libQt5Network.dll.a
          ls -la /mingw64/lib/libQt5DBus.dll.a
          ls -la /mingw64/lib/libqrencode.dll.a

          pkg-config --modversion Qt5Core
          pkg-config --libs Qt5Core
          pkg-config --libs Qt5Widgets
          pkg-config --libs Qt5Network

      - name: Autogen (regenerate autotools)
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          # 만약 repo에 깨진 libtool이 커밋돼 있으면 여기서도 오염될 수 있으니
          # autogen으로 재생성 유도
          ./autogen.sh

          # libtool 손상 체크: "| |" 토큰 나오면 즉시 실패
          if [ -f libtool ]; then
            if grep -nE '\|\s*\|' libtool >/dev/null; then
              echo "ERROR: libtool is corrupted (contains '| |')."
              echo "---- libtool context ----"
              grep -nE '\|\s*\|' -n libtool | head -n 20 || true
              exit 1
            fi
          fi

      - name: Configure (Qt GUI)
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          export PKG_CONFIG="/mingw64/bin/pkg-config"
          export PKG_CONFIG_PATH="/mingw64/lib/pkgconfig:/mingw64/share/pkgconfig"

          MOC=/mingw64/bin/moc.exe
          UIC=/mingw64/bin/uic.exe
          RCC=/mingw64/bin/rcc.exe
          if [ -x /mingw64/bin/lrelease-qt5.exe ]; then
            LRELEASE=/mingw64/bin/lrelease-qt5.exe
          else
            LRELEASE=/mingw64/bin/lrelease.exe
          fi

          MOC="$MOC" UIC="$UIC" RCC="$RCC" LRELEASE="$LRELEASE" \
            ./configure \
              --host=x86_64-w64-mingw32 \
              --with-gui=qt5 \
              --with-qt-incdir=/mingw64/include/qt5 \
              --with-qt-libdir=/mingw64/lib \
              --with-qt-bindir=/mingw64/bin \
              --disable-gui-static \
              --disable-wallet \
              --disable-tests \
              --disable-bench \
              --disable-fuzz-binary \
              --disable-zmq

      - name: Force-remove static flags from Qt link only
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          # 핵심: Qt 타겟 링크라인에서 -all-static / -static / -Wl,-Bstatic 같은 "정적 강제"만 제거
          # (libtool 파일 자체는 절대 수정 금지)
          for f in src/qt/Makefile src/Makefile; do
            if [ -f "$f" ]; then
              sed -i 's/-all-static//g' "$f"
              sed -i 's/ -static / /g' "$f"
              sed -i 's/-Wl,-Bstatic//g' "$f"
              sed -i 's/-Wl,--whole-archive//g' "$f"
              sed -i 's/-Wl,--no-whole-archive//g' "$f"
            fi
          done

          # 링크라인에 Qt libdir(-L/mingw64/lib) 강제 포함되었는지 점검 로그
          grep -RIn -- "qt/bitcoin-qt.exe" -n src/qt/Makefile 2>/dev/null || true
          grep -RIn -- "mingw64/lib" -n src/qt/Makefile 2>/dev/null || true

      - name: Build bitcoin-qt.exe (show link line)
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"
          cd src

          # 실패 시 진단용: 링커가 실제로 뭘 찾는지 출력
          make -j1 V=1 qt/bitcoin-qt.exe

          ls -la qt/bitcoin-qt.exe || true
          x86_64-w64-mingw32-objdump -p qt/bitcoin-qt.exe | grep -i "DLL Name" || true

      - name: Package (windeployqt + zip)
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          export PATH="/mingw64/bin:$PATH"

          if [ -x /mingw64/bin/windeployqt-qt5.exe ]; then
            WINDEPLOYQT=/mingw64/bin/windeployqt-qt5.exe
          else
            WINDEPLOYQT=/mingw64/bin/windeployqt.exe
          fi

          rm -rf dist
          mkdir -p dist
          cp -a src/qt/bitcoin-qt.exe dist/bitcoin-qt.exe

          "$WINDEPLOYQT" --release --no-translations --compiler-runtime dist/bitcoin-qt.exe

          powershell -NoProfile -Command "Compress-Archive -Path dist\* -DestinationPath bitcoinbt-qt-win64.zip"
          ls -la bitcoinbt-qt-win64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bitcoinbt-qt-win64
          path: bitcoinbt-qt-win64.zip
